# Dependencies
node_modules/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Logs / temp
*.log

# zg runtime artifacts (indexes live in <workspace>/.zvec-grep, not here)
# (the project config .zvec-grep/config.json lives there too — local-only, like pi-shepherd's)
.zvec-grep/
