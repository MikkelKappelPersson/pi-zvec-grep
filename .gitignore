# Dependencies
node_modules/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Logs / temp
*.log

# zg runtime artifacts (indexes live in <workspace>/.zvec-grep, not here)
# (the self-contained project config .zvec-grep/config.json is meant to be
# committed; re-include it in consuming repos: `.zvec-grep/*` + `!.zvec-grep/config.json`)
.zvec-grep/
